{! first insert layout !}
{>"layout"/}

{! then update content !}
{<bodyContent}
	<h1>{title}</h1>
	<h3>check dustjs-helpers still work (must be 20) : {@math key="16" method="add" operand="4"/}</h3>
	<p>MyHelper (must be 'myHelper rocks !') : {@myHelper /}</p>
{/bodyContent}